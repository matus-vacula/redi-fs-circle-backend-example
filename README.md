# Backend demo

This example repository shows how to use npm workspaces to separate the backend and frontend application.

To run backend:
```
npm start --workspace backend
```

To run frontend:
```
npm start --workspace frontend
```
