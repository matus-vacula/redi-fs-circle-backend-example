{
    "name": "backend",
    "version": "1.0.0",
    "main": "src/index.js",
    "scripts": {
        "start": "node index.js",
        "build": "cp -r src build/ && cp package.json build/ && cp package-lock.json build/ && cd build && npm install --production"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2"
    }
}
