{
    "name": "backend",
    "version": "1.0.0",
    "main": "src/server.js",
    "scripts": {
        "start": "node server.js",
        "build": "cp -r src dist/ && cp package.json dist/ && cp package-lock.json dist/ && cd dist && npm install --production"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2"
    }
}
